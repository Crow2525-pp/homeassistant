type: vertical-stack
cards:
  - type: heading
    heading: "Humidity"
    icon: mdi:water-percent

  - type: glance
    show_state: true
    card_mod:
      style: |
        ha-card {
          border-radius: 12px;
          box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
          transition: all 0.3s ease;
        }
        ha-card:hover {
          transform: translateY(-2px);
          box-shadow: 0 6px 12px rgba(0, 0, 0, 0.15);
        }
    entities:
      # Living Room
      - entity: climate.living_room_ac_2
        attribute: current_humidity
        name: Living Room
        tap_action:
          action: more-info
        card_mod:
          style: |
            :host {
              --paper-item-icon-color:
                {% if state_attr('climate.living_room_ac_2', 'current_humidity') | float(0) < 40 %}
                  linear-gradient(145deg, #4F46E5 0%, #3730A3 100%)
                {% elif state_attr('climate.living_room_ac_2', 'current_humidity') | float(0) > 60 %}
                  linear-gradient(145deg, #E11D48 0%, #BE123C 100%)
                {% else %}
                  linear-gradient(145deg, #0F766E 0%, #115E59 100%)
                {% endif %};
            }

      # Henry's Room
      - entity: climate.study_room_ac
        attribute: current_humidity
        name: Lucas
        tap_action:
          action: more-info
        card_mod:
          style: |
            :host {
              --paper-item-icon-color:
                {% if state_attr('climate.study_room_ac', 'current_humidity') | float(0) < 40 %}
                  linear-gradient(145deg, #4F46E5 0%, #3730A3 100%)
                {% elif state_attr('climate.study_room_ac', 'current_humidity') | float(0) > 60 %}
                  linear-gradient(145deg, #E11D48 0%, #BE123C 100%)
                {% else %}
                  linear-gradient(145deg, #0F766E 0%, #115E59 100%)
                {% endif %};
            }

      # Otto's Room
      - entity: climate.kids_room_ac
        attribute: current_humidity
        name: Otto
        tap_action:
          action: more-info
        card_mod:
          style: |
            :host {
              --paper-item-icon-color:
                {% if state_attr('climate.kids_room_ac', 'current_humidity') | float(0) < 40 %}
                  linear-gradient(145deg, #4F46E5 0%, #3730A3 100%)
                {% elif state_attr('climate.kids_room_ac', 'current_humidity') | float(0) > 60 %}
                  linear-gradient(145deg, #E11D48 0%, #BE123C 100%)
                {% else %}
                  linear-gradient(145deg, #0F766E 0%, #115E59 100%)
                {% endif %};
            }

      # Master Bedroom
      - entity: climate.masterbed_room_ac
        attribute: current_humidity
        name: Master
        tap_action:
          action: more-info
        card_mod:
          style: |
            :host {
              --paper-item-icon-color:
                {% if state_attr('climate.masterbed_room_ac', 'current_humidity') | float(0) < 40 %}
                  linear-gradient(145deg, #4F46E5 0%, #3730A3 100%)
                {% elif state_attr('climate.masterbed_room_ac', 'current_humidity') | float(0) > 60 %}
                  linear-gradient(145deg, #E11D48 0%, #BE123C 100%)
                {% else %}
                  linear-gradient(145deg, #0F766E 0%, #115E59 100%)
                {% endif %};
            }

      # Essendon
      - entity: sensor.essendon_airport_humidity
        name: Essendon
        tap_action:
          action: more-info
        card_mod:
          style: |
            :host {
              --paper-item-icon-color:
                {% if states('sensor.essendon_airport_humidity') | float(0) < 40 %}
                  linear-gradient(145deg, #4F46E5 0%, #3730A3 100%)
                {% elif states('sensor.essendon_airport_humidity') | float(0) > 60 %}
                  linear-gradient(145deg, #E11D48 0%, #BE123C 100%)
                {% else %}
                  linear-gradient(145deg, #0F766E 0%, #115E59 100%)
                {% endif %};
            }

  - type: heading
    heading: "Humidity Trends"
    icon: mdi:water-percent
    heading_style: subtitle

  - type: history-graph
    title: Humidity Trends
    hours_to_show: 24
    refresh_interval: 60
    entities:
      - entity: climate.masterbed_room_ac
        attribute: current_humidity
        name: Master
      - entity: climate.kids_room_ac
        attribute: current_humidity
        name: Otto
      - entity: climate.study_room_ac
        attribute: current_humidity
        name: Lucas
      - entity: climate.living_room_ac_2
        attribute: current_humidity
        name: Living Room
      - entity: sensor.essendon_airport_humidity
        name: Essendon
    card_mod:
      style: |
        ha-card {
          border-radius: 12px;
          box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
          transition: all 0.3s ease;
        }
        ha-card:hover {
          transform: translateY(-2px);
          box-shadow: 0 6px 12px rgba(0, 0, 0, 0.15);
        }

        # Living Room
        # - entity: sensor.ble_humidity_kitchen_temp_humidity_sensor
        #   name: BLE Humidity Kitchen
        # - entity: sensor.ble_humidity_front_door
        #   name: BLE Humidity Front Door

        # Henry's Room
        # - entity: sensor.henrys_room_ir_blaster_humidity
        #   name: Henry's Room IR Blaster
        # - entity: sensor.ble_humidity_a4c138a817fd
        #   name: Henry's Room
        # - entity: sensor.henrys_room_temp_humidity_sensor_humidity
        #   name: Henry's Room Temp Humidity

        # Otto's Room
        # - entity: sensor.ottos_room_temp_humidity_sensor_humidity
        #   name: Otto's Room Temp Humidity

        # Master Bedroom
        # - entity: sensor.hub_2_0be5_humidity
        #   name: Masterbed Sidebed Hub

        # Other Sensors
        # - entity: sensor.essendon_airport_humidity
        #   name: Essendon Airport
