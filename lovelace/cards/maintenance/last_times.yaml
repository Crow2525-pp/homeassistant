type: vertical-stack
cards:
  - type: custom:auto-entities
    card:
      type: entities
      title: "🔧 Maintenance Schedule"
      state_color: false
    filter:
      include:
        - domain: input_datetime and
        - label: "Maintenance"
    sort:
      method: domain
    card_mod:
      style: |
        ha-card {
          border-radius: 12px;
          transition: all 0.3s ease;
        }
        ha-card:hover {
          transform: translateY(-2px);
        }
