type: vertical-stack
cards:
  # - type: custom:auto-entities
  #   card:
  #     type: markdown
  #     title: Maintenance Dates
  #   filter:
  #     include:
  #       - domain: input_datetime
  #         label: Maintenance
  #   sort:
  #     method: name
  #   card_param: content
  #   template: true
  #   content: >
  #     {% for entity in entities %}
  #     - **{{ state_attr(entity, 'friendly_name') or entity }}:** {{ states(entity) }}
  #     {% endfor %}
  - type: markdown
    title: Maintenance Dates
    content: >
      {{ states('sensor.maintenance_dates_summary') }}
