type: vertical-stack
cards:
  - type: custom:auto-entities
    card:
      type: entities
    filter:
      include:
        - domain: Sensor and
        - label: Maintenance
    sort:
      method: friendly_name
