- type: custom:auto-entities
  card:
    type: entities
  filter:
    include:
      - domain: Sensor and
      - label: Maintenance
  sort:
    method: room

# TODO: Add last cleaned/serviced dates to utilities